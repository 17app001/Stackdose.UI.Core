<UserControl x:Class="ModelB.Demo.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="http://schemas.stackdose.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Background="{StaticResource BackgroundBrush}">
        
        <!-- Settings Content -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0">

                <!-- Parameter Settings - Moved to Top -->
                <Border Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="PARAMETER SETTINGS"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <!-- Row 1: Time Parameters -->
                        <UniformGrid Rows="1" Columns="3" Margin="0,0,0,10">
                            <core:PlcText Label="PreDry Time"
                                         Address="D100"
                                         Value="0"
                                         Margin="0,0,5,0"
                                         x:Name="PreDryTimePlcText"/>

                            <core:PlcText Label="Zone1 Time"
                                         Address="D101"
                                         Value="0"
                                         Margin="5,0,5,0"
                                         x:Name="Zone1TimePlcText"/>

                            <core:PlcText Label="Zone2 Time"
                                         Address="D102"
                                         Value="0"
                                         Margin="5,0,0,0"
                                         x:Name="Zone2TimePlcText"/>
                        </UniformGrid>

                        <!-- Row 2: CDA Parameters -->
                        <UniformGrid Rows="1" Columns="3" Margin="0,0,0,10">
                            <core:PlcText Label="CDA Flow"
                                         Address="D103"
                                         Value="0"
                                         Margin="0,0,5,0"
                                         x:Name="CdaFlowPlcText"/>

                            <core:PlcText Label="CDA Volume"
                                         Address="D104"
                                         Value="0"
                                         Margin="5,0,5,0"
                                         x:Name="CdaVolumePlcText"/>

                            <core:PlcText Label="Swing Speed"
                                         Address="D105"
                                         Value="0"
                                         Margin="5,0,0,0"
                                         x:Name="SwingSpeedPlcText"/>
                        </UniformGrid>

                        <!-- Row 3: Other Parameters -->
                        <UniformGrid Rows="1" Columns="3">
                            <core:PlcText Label="Batch No"
                                         Address="D106"
                                         Value="0"
                                         Margin="0,0,5,0"
                                         x:Name="BatchNoPlcText"/>
                            
                            <!-- Empty placeholders for alignment -->
                            <Border Margin="5,0,5,0"/>
                            <Border Margin="5,0,0,0"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- PLC Connection Settings -->
                <Border Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="PLC CONNECTION SETTINGS"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="IP Address:"
                                       Foreground="{DynamicResource Plc.Text.Label}"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,12"/>
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="192.168.22.39"
                                     FontSize="16"
                                     Padding="12,10"
                                     Height="40"
                                     Margin="0,0,0,12"/>

                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="Port:"
                                       Foreground="{DynamicResource Plc.Text.Label}"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,12"/>
                            <TextBox Grid.Row="1" Grid.Column="1"
                                     Text="3000"
                                     FontSize="16"
                                     Padding="12,10"
                                     Height="40"
                                     Margin="0,0,0,12"/>

                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Scan Interval (ms):"
                                       Foreground="{DynamicResource Plc.Text.Label}"
                                       FontSize="16"
                                       VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1"
                                     Text="150"
                                     FontSize="16"
                                     Padding="12,10"
                                     Height="40"/>
                        </Grid>

                        <Button Content="Save PLC Settings"
                                Background="{DynamicResource Button.Bg.Info}"
                                Foreground="White"
                                FontSize="16"
                                FontWeight="Bold"
                                Padding="12,8"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Right"
                                Width="200"
                                Click="SavePlcSettings_Click"/>
                    </StackPanel>
                </Border>

                <!-- PlcDeviceEditor Control -->
                <Border Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="PLC DEVICE EDITOR"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>
                        
                        <core:PlcDeviceEditor x:Name="DeviceEditor"/>
                    </StackPanel>
                </Border>

                <!-- System Preferences -->
                <Border Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="SYSTEM PREFERENCES"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <CheckBox Content="Enable Auto-Connect on Startup"
                                  IsChecked="True"
                                  FontSize="16"
                                  Foreground="{DynamicResource Plc.Text.Value}"
                                  Margin="0,0,0,12"/>

                        <CheckBox Content="Enable Data Logging"
                                  IsChecked="True"
                                  FontSize="16"
                                  Foreground="{DynamicResource Plc.Text.Value}"
                                  Margin="0,0,0,12"/>

                        <CheckBox Content="Enable Audit Trail"
                                  IsChecked="True"
                                  FontSize="16"
                                  Foreground="{DynamicResource Plc.Text.Value}"
                                  Margin="0,0,0,12"/>

                        <CheckBox Content="Show System Notifications"
                                  IsChecked="True"
                                  FontSize="16"
                                  Foreground="{DynamicResource Plc.Text.Value}"/>

                        <Button Content="Save Preferences"
                                Background="{DynamicResource Button.Bg.Success}"
                                Foreground="White"
                                FontSize="16"
                                FontWeight="Bold"
                                Padding="12,8"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Right"
                                Width="200"
                                Click="SavePreferences_Click"/>
                    </StackPanel>
                </Border>

                <!-- About -->
                <Border Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20">
                    <StackPanel>
                        <TextBlock Text="ABOUT"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <TextBlock FontSize="16"
                                   Foreground="{DynamicResource Plc.Text.Value}"
                                   LineHeight="24">
                            <Run Text="System Name: MODEL-B DEMO SYSTEM"/><LineBreak/>
                            <Run Text="Version: 1.0.0"/><LineBreak/>
                            <Run Text="Build Date: 2025-01-29"/><LineBreak/>
                            <Run Text="Framework: .NET 8.0"/><LineBreak/>
                            <Run Text="Copyright ? 2025 Stackdose. All rights reserved."/>
                        </TextBlock>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
