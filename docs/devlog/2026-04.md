# 開發日誌 — 2026 年 4 月

> 格式說明：`feat` 新功能 / `fix` 修正 / `refactor` 重構 / `chore` 維護
> 跨 Repo 異動標記：`[Platform ↔ UI.Core]`

---

## 2026-04-24

**fix** PlcLabel StringFormat 無條件套用（不再限制 Float 或 Divisor≠1 才格式化）

- `PlcLabel.xaml.cs:UpdateValue`：條件從 `(Float || Divisor!=1)` 改為 `!string.IsNullOrEmpty(StringFormat)`

**fix** RuntimeControlFactory 補上 `dataType` 屬性映射

- `DesignRuntime/RuntimeControlFactory.cs:CreatePlcLabel`：加 `Enum.TryParse<PlcDataType>` 映射
- 背景：PLC 存 IEEE 754 float（D109+D110）需設 `"dataType": "Float"`，之前永遠是 Word

**feat** MachinePageDesigner PropertyPanel 加 DataType 選項

- `DesignerItemViewModel.cs`：加 `DataType` 屬性 + `DataTypes` 靜態陣列（Word/DWord/Float/Bit）
- `PropertyPanel.xaml`：PlcLabelTemplate 加「資料型別」ComboBox（位於 PLC 位址下方）
- `DesignTimeControlFactory.cs`：同步加 `dataType` 映射，設計器預覽也正確

**fix** scaffold init-shell-app.ps1 三個 bug 修正

- `\$_` 語法錯誤（Unix 跳脫，PS 不認識）→ 改 `$_`
- `SampleCustomHandler` 介面簽名過時（`Execute(BehaviorAction, PlcEventContext)`）→ 改 `Execute(BehaviorActionContext ctx)`
- `MainWindow.xaml.cs` / `SampleCustomHandler.cs` 中文亂碼 → 改用 `WriteAllText + UTF8Encoding(BOM=true)`

**fix** scaffold 腳本本身加 UTF-8 BOM（根本原因修正）

- `scripts/init-shell-app.ps1`、`scripts/new-app.ps1` 加 BOM
- 根本原因：`powershell.exe`（PS5）在 zh-TW Windows 讀無 BOM 的 UTF-8 腳本時用 Big5 解讀，中文在 memory 就已損壞，無論輸出時怎麼處理都是亂碼
- **規則：含中文的 `.ps1` 一律存 UTF-8 with BOM**

---

## 2026-04-23

**feat** 恢復 MachinePageDesigner 方向鍵微調移動（從 feature/copilot b57b6e9 補入）

- `FreeCanvas.xaml`：加 `Focusable="True"` + `PreviewKeyDown="OnPreviewKeyDown"`
- `FreeCanvas.xaml.cs`：加 `OnPreviewKeyDown`（Arrow=1px / Shift+Arrow=10px）+ `FocusCanvas()`；`OnCanvasMouseDown` 加 `Focus()`
- `FreeCanvasItem.xaml.cs`：`OnMouseDown` 加 `FindFreeCanvas()?.FocusCanvas()`；加 `FindFreeCanvas()` helper
- 此功能原在 `feature/copilot` 分支（4/20），從未合入 master 或 refactor 分支，補齊缺漏

**fix** Designer 畫布尺寸不再有上下限制（移除 400–4000 / 300–3000 clamp）

**feat** DashboardShellStrategy 實作 + init-shell-app.ps1 scaffold 支援 Dashboard 模式

- 新增 `Stackdose.App.ShellShared/Services/DashboardShellStrategy.cs`：精簡生產模式，`Wrap()` 直接回傳 viewport（不包 Shell Chrome）；`IShellStrategy.LayoutMode = "Dashboard"`
- `DesignRuntime/MainWindow.xaml.cs`：`ApplyShellStrategy` 加入 Dashboard 分支 — 隱藏開發者面板、`SizeToContent=WidthAndHeight`、`ResizeMode=NoResize`、從 `DesignMeta.PlcIp/PlcPort/ScanInterval` 自動連線、`dashboardPlcHost` 顯示連線燈
- `scripts/init-shell-app.ps1` Dashboard scaffold 支援（四處修改）：
  - `[ValidateSet]` 加入 `"Dashboard"`
  - MainWindow.xaml：無 Shell Chrome，`SizeToContent="WidthAndHeight"`，`ResizeMode="NoResize"`，`DashboardHost` + `dashboardPlcHost`
  - MainWindow.xaml.cs：讀 `app-config.json` 自動連 PLC，Canvas 直接 render 進 `DashboardHost`，BehaviorEngine 接線
  - machinedesign.json 樣板：`"mode": "Dashboard"` 正確寫入

**使用方式：**
```powershell
powershell -NoProfile -File .\scripts\new-app.ps1 -AppName "Stackdose.App.DashboardTest" -Mode Dashboard -DestinationRoot .
```

**fix** Dashboard scaffold 視窗可拖移 + IP:Port 連線燈

- XAML：底部狀態列加 `MouseLeftButtonDown="OnBarDrag"`；`dashboardPlcHost` 改為 1×1 不可見佔位（保留在 visual tree 以觸發 AutoConnect）；加 `Ellipse x:Name="plcDot"`（初始紅色）+ `TextBlock x:Name="plcLabel"`（顯示 IP:Port）+ 關閉 ✕ 按鈕
- CS：`OnLoaded` 設定 `plcLabel.Text = ip:port`；`ConnectionEstablished` 回調把 dot 和 label 改為綠色；加 `OnBarDrag`（`DragMove()`）和 `OnCloseClick`（`Close()`）

**fix** MachinePageDesigner 補上 Dashboard 模式完整支援（漏實作，補齊）

- `MainViewModel.cs`：
  - `LayoutModes` 加入 `"Dashboard"`，ComboBox 現在可選
  - 新增私有欄位 `_plcIp` / `_plcPort` / `_scanInterval` + 對應公開屬性 `PlcIp` / `PlcPort` / `ScanInterval`
  - 新增 `IsDashboardMode` 計算屬性（供 XAML Visibility binding）
  - `LayoutMode` setter 加 `N(nameof(IsDashboardMode))` 通知
  - `LoadDocumentIntoUI` / `SyncUIToDocument` 補上三個 meta 欄位讀寫
- `MainWindow.xaml`：
  - 加 `Window.Resources` → `BooleanToVisibilityConverter`
  - 工具列加 PLC IP / Port / Scan(ms) 輸入欄位，`Visibility` 綁定 `IsDashboardMode`（非 Dashboard 模式自動隱藏）

---

## 2026-04-22

**refactor** Models 搬移：MachinePageDesigner → ShellShared

- `Stackdose.App.ShellShared/Models/`：DesignDocument / DesignMeta / DesignerItemDefinition / PageDefinition / PageLayoutConfig 從 `MachinePageDesigner/Models/` 搬入
- `Stackdose.App.ShellShared/Services/DesignFileService.cs`：從 `MachinePageDesigner/Services/` 搬入
- MachinePageDesigner.csproj 加入 ShellShared ProjectReference，移除本地 Models/Services

**feat** LiveRecord 即時數據記錄功能

- `LiveRecordContext.cs`（新，UI.Core/Helpers/）：全局靜態管理器；Timer 定期掃描所有已註冊 PlcLabel，對啟用記錄且有效值者呼叫 `SqliteLogger.EnqueueLiveRecord`；WeakReference 防止記憶體洩漏
- `SqliteLogger.cs`：新增 `LiveRecords` 資料表（Id / Timestamp / Address / Label / Value / IntervalSec）、`LiveRecordEntry` 私有類別、`_liveRecordQueue`、`InsertLiveRecordsSql`、`_totalLiveRecords` 統計
- `ComplianceContext.GetBatchStatistics()`：回傳元組擴充 `LiveRecords` 與 `PendingLiveRecords` 兩個欄位
- `PlcLabel.xaml.cs`：新增 DP #22 `EnableLiveRecord`（預設 `true`）；`OnPlcControlLoaded` 呼叫 `LiveRecordContext.Register`；`OnPlcControlUnloaded` 呼叫 `Unregister`
- `RuntimeControlFactory.cs`：`CreatePlcLabel` 讀 `enableLiveRecord` prop 並設定

**feat** DesignTimeControlFactory + PropertyPanel 補齊 PlcLabel 屬性

- `DesignTimeControlFactory.cs`：
  - 新增 `StaticLabel` case → `CreateStaticLabel()`（TextBlock，讀 staticText / staticFontSize / staticFontWeight / staticTextAlign / staticForeground）
  - PlcLabel factory 補讀：`LabelFontSize` / `LabelForeground` / `FrameBackground` / `LabelAlignment` / `ValueAlignment`
- `DesignerItemViewModel.cs`：新增屬性 LabelFontSize / LabelAlignment / ValueAlignment / LabelForeground / FrameBackground / StaticText / StaticFontSize / StaticFontWeight / StaticTextAlign / StaticForeground / EnableLiveRecord；key→propName mapping 同步更新
- `PropertyPanel.xaml`：
  - PlcLabel 面板加入：數值對齊（RadioButton）/ 標籤字體大小（Slider）/ 標籤顏色（ComboBox）/ 標籤對齊（RadioButton）/ 框架背景色（ComboBox）/ 啟用即時紀錄（CheckBox）
  - 新增 `StaticLabelTemplate`（文字 / 字體大小 Slider / 粗細 RadioButton / 對齊 RadioButton / 顏色 Hex TextBox）
  - `PropertyPanel` 掛接 `StaticLabelTemplate`

**chore** 新增 `scripts/new-app.ps1`：init-shell-app.ps1 的薄包裝，預設啟用 `-JsonDrivenApp`，支援 `-Mode SinglePage/Standard` 參數

---

## 2026-04-21 (B8)

**docs(B8)** 全面對齊文件，B0–B8 重構收尾

- 新增 `docs/kb/behavior-system.md`：JSON events[] schema、6個Handler、BehaviorEngine使用方式、Standard多頁Navigate
- 新增 `docs/kb/foundation-base-classes.md`：CyberControlBase/PlcControlBase繼承樹、新控件撰寫範本、PlcEventContext B2升級說明
- 更新 `docs/kb/controls-reference.md`：基類現況改為「✅ B1已完成遷移」；移除B3/B4待辦備註
- 更新 `docs/kb/architecture.md`：§2.6 PlcEventContext反映B2升級；§4.2 Shell策略接線完成；§6基類體系改為完成態；新增§7 Behavior Engine；§8技術債重新編號
- 更新 `CURRENT_FOCUS.md`：標記重構全部完成，列出B0–B8 commit對照
- 更新 auto-memory：`project_architecture.md` 加入PlcControlBase/ShellStrategy/BehaviorEngine；`project_components.md` 加入所有新增類別

---

## 2026-04-21 (B7)

**refactor(B7)** DesignRuntime Standard 模式收尾 — LeftNav 多頁導覽接線

- `PageDefinition.cs`（新增，MachinePageDesigner/Models/）：id / title / canvasItems[]，Standard 多頁定義單元
- `DesignDocument.pages[]`（新增欄位）：空清單向後相容舊 JSON
- `DesignRuntime.csproj`：直接引用 `Stackdose.UI.Templates`（MainContainer / NavigationItem）
- `MainWindow.xaml.cs`：
  - `ApplyShellStrategy()` 改為回傳 `bool`（true = 多頁模式，已自行處理 BindDocument）
  - 新增 `SetupMultiPageNavigation()`：為每個 PageDefinition 建立獨立 Canvas，建立 `ObservableCollection<NavigationItem>`，wire `NavigationRequested` + `BehaviorEngine.Navigator`，顯示第一頁
  - Navigate 動作（`"action": "Navigate", "page": "..."`）在 DesignRuntime 現可實際切換頁面

---

## 2026-04-22 (B6)

**refactor(B6)** Designer UI — PropertyPanel 事件編輯 Tab

- `MachinePageDesigner/Views/PropertyPanel.xaml`：最外層改為 TabControl，原屬性編輯內容放入 "屬性" Tab，新增 "事件 ⚡" Tab 包含 EventsPanel
- `MachinePageDesigner/Views/EventsPanel.xaml/.cs`：3 層 Master-Detail 事件編輯 UI
  - 上半：事件清單（OnType、觸發說明）+ 新增/移除按鈕
  - 下半：事件詳情（On 下拉、When 條件、Do 動作清單 + 動作詳情）
  - `_suppressHandlers` 機制防止程式碼寫 UI 時觸發 Change 回呼
- `MachinePageDesigner/ViewModels/BehaviorEventViewModel.cs`：包裝 BehaviorEvent，靜態 OnTypes / WhenOps 清單供 ComboBox 綁定
- `MachinePageDesigner/ViewModels/BehaviorActionViewModel.cs`：包裝 BehaviorAction，ShowTarget / ShowProp / ShowValue / ShowMessage / ShowTitle / ShowPage 可見性屬性
- `MachinePageDesigner/ViewModels/DesignerItemViewModel.cs`：加 `Events` ObservableCollection，`BuildEventsCollection()` 同步 CollectionChanged → `_definition.Events`


---

## 2026-04-22

**refactor(B5)** Behavior Engine — 事件驅動行為執行系統（commit `34d9c1f`）

架構設計：
- 引入 `BehaviorEngine`（ShellShared）作為 B5 核心，負責：訂閱 PLC valueChanged 事件 + SecuredButton click 事件 → 評估 `when` 條件 → 依序執行 `do` 動作清單
- `BehaviorEventBus`（UI.Core 靜態類別）：讓 SecuredButton 在不依賴 ShellShared 的前提下向 Engine 回報 click 事件
- `ControlRuntimeTag`（存在 `FrameworkElement.Tag`）：橋接 WPF 控件實例與 BehaviorEngine，提供 PropSetters 閉包（避免 Engine 直接做 reflection）
- `IControlWithBehaviors`（UI.Core）：讓 BehaviorEngine 只依賴最小介面，不依賴具體 `DesignerItemDefinition` 類別，打破循環依賴

解決循環依賴問題：
- 原始依賴鏈：`ShellShared → MachinePageDesigner → DeviceFramework → ShellShared`（循環）
- 解法：將 `BehaviorModels`（BehaviorEvent / BehaviorCondition / BehaviorAction）移至 `UI.Core.Models`
- `MachinePageDesigner/BehaviorModels.cs` 改為 `global using` 別名保持向後相容
- `DesignerItemDefinition` 實作 `IControlWithBehaviors`；`BehaviorEngine.BindDocument` 接受介面

6 個內建 Handler：
- `SetPropHandler`：透過 `ControlRuntimeTag.PropSetters` 閉包更新控件屬性（背景色、前景色、文字）
- `WritePlcHandler`：呼叫 `IPlcManager.WriteAsync("{addr},{value}")`
- `LogAuditHandler`：寫入稽核日誌（優先 AuditLogger 委派，次選 ComplianceContext.LogSystem）
- `ShowDialogHandler`：呼叫 `CyberMessageBox.Show()`
- `NavigateHandler`：呼叫 Navigator 委派（B7 完成後接入真實頁面導航）
- `SetStatusHandler`：記錄狀態變更（B6 後可接入 MachineState）

DesignRuntime 接線：
- `RuntimeControlFactory`：`Create()` 呼叫 `AttachBehaviorTag`，為所有控件建立 `ControlRuntimeTag`
- `SecuredButton`：新增 `BehaviorId` DependencyProperty；驗證通過後 `Fire(BehaviorId, "click")`
- `MainWindow`：持有 `BehaviorEngine` 欄位、`BindDocument` 在 `RenderDocument` 末尾呼叫、連線建立時設 `PlcManager`、Closing 時 `Dispose`

---

## 2026-04-21

**fix(sln)** Visual Studio 開啟方案一片空白問題（commit `623335f`）
- 兩個 `.sln` 均修正：`VisualStudioVersion = 18.x` → `17.14.36705.20`（與已安裝 VS 2022 對齊）
- 移除 `Project...EndProject` 之間的 `#` 裝飾注釋（格式不合法，導致 VS parser 出錯）
- `Stackdose.UI.Core.sln` 加入 `Stackdose.App.Monitor` 專案（App.Monitor 在 `b0e424d` 加入 repo 但未加入方案）

**refactor(B1+B2)** 基類統一 + 事件能力收斂（commit `b0e424d`）
- 新增 `PlcValueChangedEventArgs` 至 `Helpers/`（移出 PlcLabel，改為共用）
- 升級 `PlcControlBase`：加 `ValueChanged` event bus、`RaiseValueChanged()`、`CurrentValue` 屬性、`OnPlcConnected` / `OnPlcDataUpdated` 抽象方法、`OnGlobalStatusChanged` 熱更新
- 升級 `PlcEventContext`：加 `ControlValueChanged` static event + `PublishControlValueChanged()`（供未來 B5 Behavior Engine 訂閱）
- 5 個控件完成遷移（PlcLabel / PlcText / PlcStatusIndicator / AlarmViewer / SensorViewer）
  - 全部改用 `OnPlcControlLoaded` / `OnPlcControlUnloaded` / `OnPlcConnected` / `OnPlcDataUpdated` 覆寫
  - 移除各控件手動訂閱 PlcStatus 的樣板程式碼（約 -150 行）
  - 修正 PlcLabel.OnThemeChanged 遺漏 `override`（CS0114）
  - 移除 PlcText 與基類重複的 `SafeInvoke`（CS0108）
- XAML：5 個控件根元素從 `<UserControl>` 換成 `<base:PlcControlBase>`
- 編譯：Stackdose.UI.Core **0 errors、0 warnings**；所有相依專案全過

**fix(runtime)** DesignRuntime 執行測試修復 3 批（commits `6b6636e` `c3d2ba5` `0d4a639`）
- **PlcLabel circle DataTrigger regression**：B1 遷移後 5 個 DataTrigger 的 `RelativeSource Self` 指向巢狀 Border/Grid/TextBlock，不是 PlcControlBase 根元素 → 改為 `AncestorType=UserControl`
- **Circle 內容溢出**：`FrameBorder` 缺少 `ClipToBounds="True"`；`ContentGrid` 缺少 `VerticalAlignment="Center"`
- **StaticLabel 未知類型**：`RuntimeControlFactory` switch 缺少 `StaticLabel` case；補齊正確 key（`staticText` / `staticFontSize` / `staticFontWeight` / `staticTextAlign` / `staticForeground`）
- **PlcLabel 值文字不置中**：factory `CreatePlcLabel` 未讀 `valueAlignment` / `labelAlignment` / `labelFontSize` / `labelForeground` / `frameBackground` → 全部補齊
- **連線按鈕白色**：`btnConnect` disabled 狀態套用系統白底 → 加自訂 ControlTemplate 顯示深綠色

**feat(runtime)** ValueChanged 事件監測 ToggleButton（commit `8494b36`）
- 狀態列加入「📡 偵測值變化」ToggleButton
- 訂閱 `PlcEventContext.ControlValueChanged`，以 `⚡ Address = Value` 格式即時顯示於 `lblEventLog`

**feat(B3)** Shell 策略化（commit `70b919f`）
- 主旨呼應：「不懂 XAML 的工程師只需在 JSON 設定 `shellMode` 即可選擇 Shell 外觀」
- `DesignDocument` 加 `shellMode`（`"FreeCanvas"` / `"SinglePage"` / `"Standard"`，向後相容）
- `ShellShared` 新增 `IShellStrategy` 介面 + `ShellStrategyFactory`
- 三個策略實作：`FreeCanvasShellStrategy`（裸畫布）、`SinglePageShellStrategy`（SinglePageContainer）、`StandardShellStrategy`（MainContainer，B7 完成導覽接線）
- `DesignRuntime` 加入 ShellShared 參考；載入文件後 `ApplyShellStrategy()` 自動切換
- 狀態列加入 `Shell: FreeCanvas/SinglePage/Standard` 即時指示

**feat(B4)** Behavior JSON Schema（commit `4a8cc13`）
- 主旨呼應：「工程師只需在 JSON 描述『數值 > 100 時做什麼』，不需寫 XAML Trigger」
- 新增 `BehaviorModels.cs`：三個 POCO — `BehaviorEvent`（on/when/do）、`BehaviorCondition`（op/value）、`BehaviorAction`（action/target/prop/value/message/title/page）
- `DesignerItemDefinition.Events` 預設空清單，舊 JSON 完全向後相容
- `Clone()` 完整深複製 events（含巢狀 do 動作列表）
- docs: `designer-system.md` 補齊 JSON 範例、events 欄位規格表、動作類型說明
- `CreatePlcText` 補讀 `showSuccessMessage` / `enableAuditTrail` from JSON
- PROGRESS.md 補全 runtime fix session commits（`6b6636e`~`8494b36`）
- devlog 補 runtime 修復細節
- 新分支：`refactor/foundation-and-behavior`
- 觸發背景：Dashboard 模式完成後發現 PlcLabel 等控件無法表達「值變化時的反應行為」；追溯到底層 4 個結構問題（無共用基類、無統一控件事件出口、Shell 無策略化、文件漂移）
- 最終主旨：「讓不懂 XAML 的工程師或設計師，也能為 PLC 工業機台做出符合 FDA 21 CFR Part 11 稽核要求的監控介面」
- 方法：底層優先（B0–B3）→ Behavior 層（B4–B5）→ Designer 層（B6–B7）→ 文件回灌（B8）

**docs** 重構 meta 文件建立
- 新增 `docs/refactor/` 資料夾：README / PLAN / PROGRESS / HANDOFF 四份
- 產出 `docs/refactor/B0-control-inventory.md`：24 Core + 17 Templates 控件實況盤點
- 產出 `docs/refactor/B0-findings.md`：文件 vs 實況差異 20 條

**docs** 底層文件校正（B0.4 / B0.5）
- 改寫 `docs/kb/controls-reference.md`：補 Templates 16 元件、補 PrintHeadStatus/PlcEventTrigger 等缺漏、修 PlcDataGridPanel 所屬專案、基類狀態改為「已寫未用」
- 改寫 `docs/kb/architecture.md`：§2 新增 PlcEventContext、§4 補 Templates Shell 實作現況、§5 修 PLC 輪詢路徑、§6 基類體系改為「規劃中」
- 更新 `docs/PROJECT_MAP.md`：補 DesignPlayer 依賴
- 更新 `CLAUDE.md`：專案元件數量精確化（UI.Core 26 / UI.Templates 16）
- 更新 `CURRENT_FOCUS.md`：改指向 `docs/refactor/` 路線圖

---

## 2026-04-14

**feat** DesignRuntime 新增模擬器模式與亂數 D100~D102 測試注入
- 新增「模擬器模式」CheckBox（啟用後不需真實 PLC 即可連線）
- 新增「亂數 D100~D102」按鈕（每 100~300ms 隨機寫入測試值，Toggle 開關）
- 修正斷線時未清除 `PlcContext.GlobalStatus` 問題
- 載入 JSON 後呼叫 `RefreshMonitors()` 確保新地址加入掃描清單

**chore** 方案分組整理 + 聚焦方案 + 目前焦點文件
- `Stackdose.UI.Core.sln` 加入 Solution Folder 分組（5個群組）
- 新增 `Stackdose.Designer.sln`（設計器聚焦方案，14個專案，每日開發用）
- 新增 `CURRENT_FOCUS.md`（當下焦點 + 各專案一句話說明）

**docs** 建立知識庫系統
- 新增 `CLAUDE.md`（根目錄快速上下文）
- 新增 `docs/PROJECT_MAP.md`（完整依賴圖）
- 新增 `docs/kb/`（架構、設計器、Platform契約、控制項參考）
- 新增 `docs/devlog/`（開發日誌月份檔案）
- 新增 `docs/MANAGER_BRIEF.md`（技術主管進度簡報）
- 清除 7 個散落冗餘的 .md 檔案

---

## 2026-04-13

**feat** 新增 DesignRuntime 真實執行專案（PLC 連線 + JSON 載入）
- 新增 `Stackdose.App.DesignRuntime` 專案至方案
- 支援載入 `.machinedesign.json` 並連線真實 PLC 顯示即時數值

**feat** 新增 DesignViewer 測試專案（拖入 JSON 即時預覽畫布）
- 新增 `Stackdose.Tools.DesignViewer` 專案至方案
- 拖入 `.machinedesign.json` 即時渲染，不需連 PLC

**feat** 設計器全功能整合（複製貼上 / 多選同步 / GroupBox / 對齊分配）
- MachinePageDesigner 新增 Ctrl+C / Ctrl+V 複製貼上
- 多選後同步移動
- 支援 GroupBox 群組化
- 新增對齊與均等分配功能

**feat** MachinePageDesigner 全面改為自由畫布拖曳設計器
- 正式棄用 Zone 制模式，統一改為 FreeCanvas 模式
- 控制項可拖曳至畫布任意位置

**feat** 畫布尺寸設定 + 修正儲存讀取與 Undo 正確性
- 新增畫布寬高設定功能
- 修正多個儲存/載入後狀態不一致問題
- Undo/Redo 正確性改善

**fix** FreeCanvasItem 無法移動且卡死問題
- 修正拖曳事件捕獲導致的卡死 bug

**feat** 自由畫布新增 Snap / Z-Order / 框選 / 鎖定四大功能
- Snap：拖曳時自動吸附格線
- Z-Order：調整控制項層級（前置/後置）
- 框選：拖曳框選多個控制項
- 鎖定：防止已完成控制項被誤移

**refactor** MachinePageDesigner 統一改為自由畫布（FreeCanvas）模式
- 移除 Zone 制 Grid 排列邏輯
- 以 `FreeCanvasItem` 包裝所有控制項

---

## 2026-04-08

**feat** MachinePageDesigner 完成拖曳體驗與 DeviceFramework 整合
- 拖曳操作體驗優化（MouseCapture、Cursor 反饋）
- 與 DeviceFramework 的 `DeviceLabelViewModel` 整合

**feat** 實作自由畫布設計器（FreeCanvas）完整功能
- FreeCanvas 首版完整功能實作
- 包含：放置、移動、刪除、屬性面板編輯

---

## 2026-04-07

**fix** 完成 bug 修正
- 修正數個設計器操作相關問題（詳情見 commit 89fc943）

---

## 2026-04-01

**feat** 抽出 PlcDataGridPanel 通用元件，取代 DynamicDevicePage 內嵌 panel
- 將原本嵌在 `DynamicDevicePage` 的 Labels 面板抽成獨立 `PlcDataGridPanel` 控制項
- 可在其他頁面重用

**feat** ProjectGeneratorUI 預覽改用真實 PlcDataGridPanel 顯示 Labels 資料
- 產生器預覽頁面改用真實控制項渲染，更貼近實際效果

**fix** ProjectGeneratorUI App.xaml 補上 UI.Core Theme.xaml 資源合併
- 修正 ProjectGeneratorUI 缺少主題資源導致控制項樣式異常

**docs** 新增 MachinePageDesigner 架構書（初版規劃文件）

**chore** 新增 `Stackdose.Tools.MachinePageDesigner` 專案（初始化，Zone制模式，開發中）

**chore** 清理冗餘說明檔與備份腳本，新增方案說明文件
