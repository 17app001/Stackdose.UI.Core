<UserControl x:Class="ModelB.Demo.Pages.DeviceControlPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="http://schemas.stackdose.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Title -->
        <Border Grid.Row="0"
                Background="{DynamicResource Plc.Bg.Main}"
                BorderBrush="{DynamicResource Plc.Border}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="DEVICE CONTROL"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{DynamicResource Cyber.Text.Main}"
                           FontFamily="Consolas"/>
                <TextBlock Text="Control print head and system parameters"
                           FontSize="14"
                           Foreground="{DynamicResource Plc.Text.Label}"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Print Head Controller -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Print Head Controller -->
                <Border Grid.Row="0"
                        Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="PRINT HEAD CONTROLLER"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <core:PrintHeadController/>
                    </StackPanel>
                </Border>

                <!-- Simulator Control Panel (Optional) -->
                <Border Grid.Row="1"
                        Background="{DynamicResource Sensor.Bg.Main}"
                        BorderBrush="{DynamicResource Sensor.Border}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16">
                    <StackPanel>
                        <TextBlock Text="SIMULATOR CONTROL PANEL"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontFamily="Consolas"
                                   Margin="0,0,0,16"/>

                        <core:SimulatorControlPanel/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
