<UserControl x:Class="Stackdose.UI.Core.Controls.PlcStatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    
    <Border Background="{DynamicResource Plc.Bg.Main}" 
            BorderBrush="{DynamicResource Plc.Border}"
            BorderThickness="1" 
            CornerRadius="5" 
            Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Status Light -->
            <Ellipse x:Name="StatusLight" 
                     Grid.Column="0"
                     Width="24" Height="24" 
                     Fill="Red" 
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"/>

            <!-- Status Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="StatusText" 
                           Text="DISCONNECTED" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Foreground="Red"/>
                <TextBlock x:Name="IpDisplay" 
                           Text="192.168.22.39:3000" 
                           FontSize="10" 
                           Foreground="{DynamicResource Cyber.Text.Muted}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
