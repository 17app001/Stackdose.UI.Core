# 開發日誌 — 2026 年 5 月

> 格式說明：`feat` 新功能 / `fix` 修正 / `refactor` 重構 / `chore` 維護
> 跨 Repo 異動標記：`[Platform ↔ UI.Core]`

---

## 2026-05-06（續）

**feat** Dashboard 視窗等比例適螢幕 + 黑色 letterbox（`06c1726`）`[UI.Core]`

- **問題：** Dashboard scaffold 視窗 titlebar 只有最小化和關閉，在小螢幕畫布超出邊界、大螢幕留白；且 letterbox 區域顯示白色背景
- **修正：** scaffold 模板（`init-shell-app.ps1`）
  - Window 加 `Background="#1E1E32"`，letterbox 顯示深色
  - 移除 `SizeToContent="WidthAndHeight"`；Row 1 改 `Height="*"`
  - `ContentPresenter` 包 `Viewbox Stretch="Uniform"`，等比例縮放至任意螢幕
  - 新增 `□` / `❐` 切換按鈕（最大化/還原）
  - `OnBarDrag` 在 Maximized 狀態下停用；`OnWindowStateChanged` 同步圖示
  - `CompensateWindowSize` 簡化（`WindowStyle="None"` 無系統邊框）
- 同步更新 `MyPrintApp/MainWindow.xaml`（補 `Background="#1E1E32"` + 已有 Viewbox）

---

## 2026-05-06

**fix** PrintHeadController 方向缺口 — direction 現在正確傳給 Feiyang SDK `[Platform ↔ UI.Core]`

- **根本原因：** `ConfigurePrintModeAsync` 只傳 DPI；方向從 JSON config 靜態讀取（`pm.PrintDirection`），UI ComboBox 的選擇只寫 PLC D-register，SDK 從未收到動態方向設定
- **修正：** `IPrintHead.ConfigurePrintModeAsync(int? imageDpi, int? direction)` 新增可選 `direction` 參數（backward-compatible，既有 `PrintHeadManager` 呼叫不受影響）；`FeiyangPrintHead.ConfigurePrintMode` fallback 仍用 config 值
- **ComboBox 擴充：** 補齊 `CarAtRight 雙向 / 左到右 / 右到左`，合計 6 個選項（對應 SDK enum 值 1/2/3/129/130/131）
- 異動：`Stackdose.Abstractions/Print/IPrintHead.cs`、`Stackdose.PrintHead/Feiyang/FeiyangPrintHead.cs`、`PrintHeadController.xaml`（+3 ComboBoxItem）、`PrintHeadController.xaml.cs`（index→SDK 值映射）

---

**fix** PrintHeadController 載入 log 補上 StartX / CaliMM 參數

- 原本 log 只顯示 DPI 和 Dir，StartX / CaliMM 雖有傳入 SDK 但未記錄
- 載入開始與傳圖成功兩條 log 均補上 `StartX` 和 `CaliMM` 欄位
- 異動：`PrintHeadController.xaml.cs`（兩處 `ComplianceContext.LogSystem` 呼叫）

---

**refactor** SystemClock 搬移 + Precision Gap 容器感知 + PrintHeadController UI 重排（`4fb3519`）`[UI.Core]`

- **SystemClock 搬移：** 從 `UI.Templates` 移入 `UI.Core`；更新所有 XAML / Factory / scaffold scripts 的引用路徑；修正 scaffolded 專案 Runtime 渲染報錯
- **Precision Gap 容器感知：** `SurgicalTidyUp` 演算法保護 Spacer 內部 nested layout，不再把子元件拉出容器邊界
- **PrintHeadController UI 重排：** 讀取/載入/取消三個按鈕移到第二排（`Grid *` 等寬），`已連線` 狀態 Badge 移至 Header 右側；移除左側舊圖片操作區塊
- **Viewer 字體統一：** AlarmViewer / SensorViewer header 字體統一為 13px
- 異動：`DesignRuntime/App.xaml`、`RuntimeControlFactory.cs`、`DesignTimeControlFactory.cs`、`MainViewModel.cs`、`PrintHeadController.xaml`、`AlarmViewer.xaml`、`SensorViewer.xaml`、`SystemClock.xaml/cs`、`scripts/init-shell-app.ps1`

---

**chore** FeiyangWrapper GUID 修正 + MyPrintApp 加入方案（`feed0c8`）`[UI.Core]`

- VS 自動將 `Stackdose.UI.Core.sln` 中 FeiyangWrapper 的 GUID 從 `{0EA55D76}` 修正為與 vcxproj 實際 GUID `{58A7CD3A}` 一致
- 將 `MyPrintApp` scaffold 產出專案加入 sln

---

**feat** FeiyangWrapper 整合：ProjectReference 混合式架構 + waveform 路徑修正（`a80bd70`→merge `338acdb`）`[Platform]`

- **背景：** WinForms 版 FeiyangWrapper.vcxproj 複製至 `D:\工作區\Project\Sdk\FeiyangWrapper\`，統一 Platform 與 UI.Core 的 C++ SDK 來源
- **混合式 csproj：** `<ProjectReference>` 加 `MSBuildRuntimeType != 'Core'` guard（VS 建置順序保障）+ `<Reference>` DLL hints（`dotnet build` 型別解析）；解決 `dotnet build` 遇 C++/CLI vcxproj 直接失敗的問題
- **waveform 路徑修正：** `FeiyangPrintHead.ResolveWaveformPath()` 多路徑搜尋（`Resources/`→`Config/waves/`→`Config/`）；從 JSON `Firmware.WaveformPath` fallback 讀取
- develop → master merge 完成，兩個 repo 均已 push

---

## 2026-05-05

**feat** MachinePageDesigner B10 設計器功能批次完成（多個 commits）`[UI.Core]`

**Spacer 容器化深層選取**（`7227130`）
- Spacer Copy/Cut/Delete 現在自動連動所有 visual children
- 修正 `CutSelected` 缺漏方法、清理 `MainViewModel` 重複程式碼
- 全域剪貼簿：主畫布 `Ctrl+C/X` 與 TabPanel editor `Ctrl+V` 互通
- TabPanel 貼上後自動重置座標至左上角（10, 10）

**Spacer / TabPanel 最終打磨**（`e6cdbc5`）
- Spacer 框線／背景顏色與 `HeaderColor` 同步連動
- TabPanel 子設計器完整接線：本地 `PropertyPanel`、快捷鍵 `Ctrl+X/C/V/Z/Y`

**Spacer 隱藏標題**（`3f4e782`）
- 新增 `showTitle` 屬性，Designer 與 Runtime 均支援
- 隱藏標題時顯示 8px 透明 Ghost Handle 維持可拖曳性
- 修正 `DesignerItemViewModel.GetPropBool` mapping bug

**全域 Gap 系統 + Gap Snapping**（`2362ff1`）
- `globalSpacing` 加入 `DesignDocument` layout config
- Spacer auto-arrange（H/V/Grid）遵守 `GlobalSpacing` padding 與間距
- SmartSnap：元件距離剛好等於 `GlobalSpacing` 時產生磁性吸附

**Canvas Padding + Precision Gap 升級**（`9539006`）
- `CanvasPadding` 加入 layout config，工具列新增 Padding 輸入欄
- `SurgicalTidyUp` 演算法重構：邊緣元件自動 snap 到 padding zone；元件間距以 `GlobalSpacing` 校正
- 新增「水平堆疊 / 垂直堆疊」一鍵整理工具
