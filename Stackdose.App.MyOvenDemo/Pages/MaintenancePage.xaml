<UserControl x:Class="Stackdose.App.MyOvenDemo.Pages.MaintenancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="http://schemas.stackdose.com/wpf"
             Background="{DynamicResource Surface.Bg.Page}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="12" CornerRadius="8" Margin="0,0,0,12"
                Background="{DynamicResource Surface.Bg.Panel}"
                BorderBrush="{DynamicResource Surface.Border.Default}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="MAINTENANCE MODE" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="{DynamicResource Accent.Warning}"/>
                <TextBlock Text="Target Machine: " VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="CboMachine" ItemsSource="{Binding MachineIds}" SelectedItem="{Binding SelectedMachineId}" MinWidth="200" Height="30" SelectionChanged="OnMachineChanged"/>
            </StackPanel>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ItemsPanel">
                <Border x:Name="Panel_OVEN1" Visibility="Collapsed" CornerRadius="8" Padding="12" Margin="4" Background="{DynamicResource Surface.Bg.Panel}" BorderBrush="{DynamicResource Surface.Border.Default}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Reflow Oven A" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <WrapPanel>
                            <Border CornerRadius="6" Padding="12,8" Margin="4" MinWidth="160" Background="{DynamicResource Surface.Bg.Card}">
                                <StackPanel>
                                    <TextBlock Text="�[���� ON/OFF" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                        <core:SecuredButton Content="ON" Tag="M600,1" Click="OnToggleClick" Theme="Success" RequiredLevel="Supervisor" MinWidth="55" Margin="0,0,4,0"/>
                                        <core:SecuredButton Content="OFF" Tag="M600,0" Click="OnToggleClick" Theme="Error" RequiredLevel="Supervisor" MinWidth="55"/>
                                    </StackPanel>
                                    <TextBlock Text="M600" FontSize="9" Margin="0,4,0,0" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </StackPanel>
                            </Border>
                            <GroupBox Header="������t" Margin="4" MinWidth="280">
                                <core:PlcDeviceEditor Label="������t" Address="D700" Value="0" Reason="Maintenance: FanSpeed" RequiredLevel="Supervisor" EnableAuditTrail="True" Height="180"/>
                            </GroupBox>
                            <Border CornerRadius="6" Padding="12,8" Margin="4" MinWidth="160" Background="{DynamicResource Surface.Bg.Card}">
                                <StackPanel>
                                    <TextBlock Text="��e�a�o��" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <core:SecuredButton Content="ConveyorJog" Tag="M601" PreviewMouseLeftButtonDown="OnMomentaryDown" PreviewMouseLeftButtonUp="OnMomentaryUp" Theme="Warning" RequiredLevel="Supervisor" MinWidth="120" Margin="0,6,0,0"/>
                                    <TextBlock Text="M601" FontSize="9" Margin="0,4,0,0" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </StackPanel>
                            </Border>
                            <Border CornerRadius="6" Padding="12,8" Margin="4" MinWidth="160" Background="{DynamicResource Surface.Bg.Card}">
                                <StackPanel>
                                    <TextBlock Text="�l�źʱ�" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <core:PlcLabel Address="D100" FontSize="20" FontWeight="Bold" DefaultValue="--" Margin="0,4,0,0" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="D100" FontSize="9" Margin="0,4,0,0" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                </Border>
                <Border x:Name="Panel_OVEN2" Visibility="Collapsed" CornerRadius="8" Padding="12" Margin="4" Background="{DynamicResource Surface.Bg.Panel}" BorderBrush="{DynamicResource Surface.Border.Default}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Reflow Oven B" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <WrapPanel>
                            <Border CornerRadius="6" Padding="12,8" Margin="4" MinWidth="160" Background="{DynamicResource Surface.Bg.Card}">
                                <StackPanel>
                                    <TextBlock Text="�[���� ON/OFF" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                        <core:SecuredButton Content="ON" Tag="M700,1" Click="OnToggleClick" Theme="Success" RequiredLevel="Supervisor" MinWidth="55" Margin="0,0,4,0"/>
                                        <core:SecuredButton Content="OFF" Tag="M700,0" Click="OnToggleClick" Theme="Error" RequiredLevel="Supervisor" MinWidth="55"/>
                                    </StackPanel>
                                    <TextBlock Text="M700" FontSize="9" Margin="0,4,0,0" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </StackPanel>
                            </Border>
                            <GroupBox Header="������t" Margin="4" MinWidth="280">
                                <core:PlcDeviceEditor Label="������t" Address="D800" Value="0" Reason="Maintenance: FanSpeed" RequiredLevel="Supervisor" EnableAuditTrail="True" Height="180"/>
                            </GroupBox>
                            <Border CornerRadius="6" Padding="12,8" Margin="4" MinWidth="160" Background="{DynamicResource Surface.Bg.Card}">
                                <StackPanel>
                                    <TextBlock Text="��e�a�o��" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <core:SecuredButton Content="ConveyorJog" Tag="M701" PreviewMouseLeftButtonDown="OnMomentaryDown" PreviewMouseLeftButtonUp="OnMomentaryUp" Theme="Warning" RequiredLevel="Supervisor" MinWidth="120" Margin="0,6,0,0"/>
                                    <TextBlock Text="M701" FontSize="9" Margin="0,4,0,0" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>