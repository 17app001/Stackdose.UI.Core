<UserControl x:Class="Stackdose.UI.Core.Controls.LogManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1400"
             d:DesignHeight="900">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Stackdose.UI.Core;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <Style x:Key="DateItemStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Plc.Bg.Main}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Plc.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Sensor.Bg.Header}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource Cyber.NeonBlue}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="LogCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Log.Bg.Main}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Log.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="0,0,0,12"/>
            </Style>

            <Style x:Key="TypeBadgeStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource Cyber.Bg.Dark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource Plc.Bg.Main}"
                BorderBrush="{DynamicResource Plc.Border}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="16"
                Margin="16,16,16,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0: Log Type Buttons -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <ToggleButton Content="AUDIT TRAIL"
                                  Background="#E85B4A"
                                  IsChecked="{Binding IsAuditTrailMode}"
                                  Click="AuditTrailButton_Click"
                                  Width="130"
                                  Height="32"
                                  Margin="0,0,8,0"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="12"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="OPERATION"
                                  Background="#3AA0FF"
                                  IsChecked="{Binding IsOperationMode}"
                                  Click="OperationButton_Click"
                                  Width="130"
                                  Height="32"
                                  Margin="0,0,8,0"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="12"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="EVENT"
                                  Background="#FFB020"
                                  IsChecked="{Binding IsEventMode}"
                                  Click="EventButton_Click"
                                  Width="130"
                                  Height="32"
                                  Margin="0,0,8,0"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="12"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="PERIODIC DATA"
                                  Background="#3AC977"
                                  IsChecked="{Binding IsPeriodicDataMode}"
                                  Click="PeriodicDataButton_Click"
                                  Width="130"
                                  Height="32"
                                  Margin="0,0,24,0"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="12"
                                  FontFamily="Consolas"/>

                    <TextBlock Text="FROM"
                               Foreground="{DynamicResource Plc.Text.Label}"
                               VerticalAlignment="Center"
                               FontSize="12"
                               FontWeight="Bold"
                               FontFamily="Consolas"
                               Margin="0,0,8,0"/>
                    
                    <DatePicker SelectedDate="{Binding FilterFromDate}"
                                Width="120"
                                Height="32"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center"/>
                    
                    <TextBlock Text="TO"
                               Foreground="{DynamicResource Plc.Text.Label}"
                               VerticalAlignment="Center"
                               FontSize="12"
                               FontWeight="Bold"
                               FontFamily="Consolas"
                               Margin="0,0,8,0"/>
                    
                    <DatePicker SelectedDate="{Binding FilterToDate}"
                                Width="120"
                                Height="32"
                                VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Query"
                            Click="QueryButton_Click"
                            Background="{DynamicResource Button.Bg.Info}"
                            Foreground="White"
                            Width="90"
                            Height="32"
                            Margin="0,0,8,0"
                            FontWeight="Bold"
                            FontSize="14"
                            FontFamily="Microsoft JhengHei"/>
                    
                    <Button Content="Export PDF"
                            Click="ExportPdfButton_Click"
                            Background="#A78BFA"
                            Foreground="White"
                            Width="90"
                            Height="32"
                            FontWeight="Bold"
                            FontSize="14"
                            FontFamily="Microsoft JhengHei"/>
                </StackPanel>

                <!-- Row 1: Event Severity Filter (only visible in Event mode) -->
                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                            Orientation="Horizontal" 
                            Margin="0,12,0,0"
                            Visibility="{Binding IsEventMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="SEVERITY FILTER:"
                               Foreground="{DynamicResource Plc.Text.Label}"
                               VerticalAlignment="Center"
                               FontSize="12"
                               FontWeight="Bold"
                               FontFamily="Consolas"
                               Margin="0,0,12,0"/>
                    
                    <ToggleButton Content="ALL"
                                  x:Name="SeverityAllBtn"
                                  IsChecked="{Binding IsSeverityAll, Mode=TwoWay}"
                                  Click="SeverityAll_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#6B7280"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="CRITICAL"
                                  x:Name="SeverityCriticalBtn"
                                  IsChecked="{Binding IsSeverityCritical, Mode=TwoWay}"
                                  Click="SeverityCritical_Click"
                                  Width="80"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#DC2626"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="MAJOR"
                                  x:Name="SeverityMajorBtn"
                                  IsChecked="{Binding IsSeverityMajor, Mode=TwoWay}"
                                  Click="SeverityMajor_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#EA580C"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="MINOR"
                                  x:Name="SeverityMinorBtn"
                                  IsChecked="{Binding IsSeverityMinor, Mode=TwoWay}"
                                  Click="SeverityMinor_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#CA8A04"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>
                    
                    <ToggleButton Content="INFO"
                                  x:Name="SeverityInfoBtn"
                                  IsChecked="{Binding IsSeverityInfo, Mode=TwoWay}"
                                  Click="SeverityInfo_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#2563EB"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>

                    <TextBlock Text="|"
                               Foreground="{DynamicResource Plc.Border}"
                               VerticalAlignment="Center"
                               FontSize="16"
                               Margin="8,0,12,0"/>

                    <TextBlock Text="EVENT TYPE:"
                               Foreground="{DynamicResource Plc.Text.Label}"
                               VerticalAlignment="Center"
                               FontSize="12"
                               FontWeight="Bold"
                               FontFamily="Consolas"
                               Margin="0,0,12,0"/>

                    <ToggleButton Content="Alarm"
                                  x:Name="EventTypeAlarmBtn"
                                  IsChecked="{Binding IsEventTypeAlarm, Mode=TwoWay}"
                                  Click="EventTypeAlarm_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#EF4444"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>

                    <ToggleButton Content="Warning"
                                  x:Name="EventTypeWarningBtn"
                                  IsChecked="{Binding IsEventTypeWarning, Mode=TwoWay}"
                                  Click="EventTypeWarning_Click"
                                  Width="75"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#F59E0B"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>

                    <ToggleButton Content="System"
                                  x:Name="EventTypeSystemBtn"
                                  IsChecked="{Binding IsEventTypeSystem, Mode=TwoWay}"
                                  Click="EventTypeSystem_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#3B82F6"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>

                    <ToggleButton Content="Safety"
                                  x:Name="EventTypeSafetyBtn"
                                  IsChecked="{Binding IsEventTypeSafety, Mode=TwoWay}"
                                  Click="EventTypeSafety_Click"
                                  Width="70"
                                  Height="28"
                                  Margin="0,0,6,0"
                                  Background="#8B5CF6"
                                  Foreground="White"
                                  FontWeight="Bold"
                                  FontSize="11"
                                  FontFamily="Consolas"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="16,0,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource Sensor.Bg.Main}"
                    BorderBrush="{DynamicResource Sensor.Border}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="{DynamicResource Sensor.Bg.Header}"
                            BorderBrush="{DynamicResource Sensor.Border}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10"
                            CornerRadius="6,6,0,0">
                        <TextBlock Text="DATE LIST"
                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   FontFamily="Consolas"/>
                    </Border>

                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="12">
                        <ItemsControl ItemsSource="{Binding DateGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource DateItemStyle}"
                                            MouseLeftButtonDown="DateItem_Click"
                                            Tag="{Binding}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="{Binding DateStr}"
                                                       Foreground="{DynamicResource Cyber.Text.Main}"
                                                       FontSize="14"
                                                       FontWeight="Bold"
                                                       FontFamily="Microsoft JhengHei"/>
                                            
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding RelativeStr}"
                                                       Foreground="{DynamicResource Plc.Text.Value}"
                                                       FontSize="12"
                                                       FontFamily="Microsoft JhengHei"
                                                       Margin="0,4,0,0"/>
                                            
                                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,0">
                                                <Ellipse Width="8" Height="8"
                                                         Fill="{DynamicResource Status.Success}"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,6,0"/>
                                                <TextBlock Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="12"
                                                           FontFamily="Microsoft JhengHei">
                                                    <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                                                    <Run Text=" Records"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Background="{DynamicResource Sensor.Bg.Main}"
                    BorderBrush="{DynamicResource Sensor.Border}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="{DynamicResource Sensor.Bg.Header}"
                            BorderBrush="{DynamicResource Sensor.Border}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10"
                            CornerRadius="6,6,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Foreground="{DynamicResource Sensor.Text.Title}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       FontFamily="Consolas"
                                       VerticalAlignment="Center">
                                <Run Text="{Binding CurrentViewModeText, Mode=OneWay}"/>
                                <Run Text=" - "/>
                                <Run Text="{Binding SelectedDateGroup.DateStr, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Foreground="{DynamicResource Plc.Text.Label}"
                                       FontSize="14"
                                       FontFamily="Microsoft JhengHei"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0">
                                <Run Text="  (Total: "/>
                                <Run Text="{Binding CurrentRecordCount, Mode=OneWay}"/>
                                <Run Text=" Records)"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="12">
                        <Grid>
                            <!-- Audit Trail Cards -->
                            <ItemsControl ItemsSource="{Binding CurrentAuditTrails}"
                                          Visibility="{Binding IsAuditTrailMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource LogCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Row="0" Grid.Column="0"
                                                           Text="Timestamp:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"/>
                                                <TextBlock Grid.Row="0" Grid.Column="1"
                                                           Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss.fff}'}"
                                                           Foreground="{DynamicResource Cyber.Text.Main}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           FontFamily="Consolas"/>

                                                <TextBlock Grid.Row="1" Grid.Column="0"
                                                           Text="User:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <TextBlock Grid.Row="1" Grid.Column="1"
                                                           Text="{Binding User}"
                                                           Foreground="{DynamicResource Plc.Text.Value}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           FontFamily="Microsoft JhengHei"
                                                           Margin="0,8,0,0"/>

                                                <TextBlock Grid.Row="2" Grid.Column="0"
                                                           Text="Action:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <Border Grid.Row="2" Grid.Column="1"
                                                        Style="{StaticResource TypeBadgeStyle}"
                                                        Background="{DynamicResource Button.Bg.Warning}"
                                                        Margin="0,8,0,0">
                                                    <TextBlock Text="{Binding Action}"
                                                               Foreground="White"
                                                               FontSize="14"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"/>
                                                </Border>

                                                <TextBlock Grid.Row="3" Grid.Column="0"
                                                           Text="Target Device:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <TextBlock Grid.Row="3" Grid.Column="1"
                                                           Text="{Binding TargetDevice}"
                                                           Foreground="{DynamicResource Cyber.Text.Main}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>

                                                <TextBlock Grid.Row="4" Grid.Column="0"
                                                           Text="Old Value:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <TextBlock Grid.Row="4" Grid.Column="1"
                                                           Text="{Binding OldValue}"
                                                           Foreground="{DynamicResource Status.Offline}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>

                                                <TextBlock Grid.Row="5" Grid.Column="0"
                                                           Text="New Value:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <TextBlock Grid.Row="5" Grid.Column="1"
                                                           Text="{Binding NewValue}"
                                                           Foreground="{DynamicResource Status.Success}"
                                                           FontSize="14"
                                                           FontWeight="Bold"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>

                                                <TextBlock Grid.Row="6" Grid.Column="0"
                                                           Text="Reason:"
                                                           Foreground="{DynamicResource Plc.Text.Label}"
                                                           FontSize="14"
                                                           FontFamily="Consolas"
                                                           Margin="0,8,0,0"/>
                                                <TextBlock Grid.Row="6" Grid.Column="1"
                                                           Text="{Binding Reason}"
                                                           Foreground="{DynamicResource Cyber.Text.Main}"
                                                           FontSize="14"
                                                           FontFamily="Microsoft JhengHei"
                                                           TextWrapping="Wrap"
                                                           Margin="0,8,0,0"/>

                                                <Border Grid.Row="7" Grid.ColumnSpan="2"
                                                        Height="4"
                                                        Background="{DynamicResource Button.Bg.Info}"
                                                        Margin="-16,12,-16,-16"
                                                        CornerRadius="0,0,8,8"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Operation Log Cards - Two Row Layout -->
                            <ItemsControl ItemsSource="{Binding CurrentOperationLogs}"
                                          Visibility="{Binding IsOperationMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource LogCardStyle}" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Row 0: Timestamp + UserId (no label) -->
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Timestamp -->
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                                               Foreground="{DynamicResource Cyber.NeonBlue}"
                                                               FontSize="14"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"
                                                               VerticalAlignment="Center"/>

                                                    <!-- User ID (direct display, no label) -->
                                                    <TextBlock Grid.Column="1" 
                                                               Text="{Binding UserId}"
                                                               Foreground="{DynamicResource Sensor.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"
                                                               VerticalAlignment="Center"/>
                                                </Grid>

                                                <!-- Row 1: Command + States + Message -->
                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="200"/>
                                                        <ColumnDefinition Width="200"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Command Name -->
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding CommandName}"
                                                               Foreground="{DynamicResource Plc.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               VerticalAlignment="Center"/>

                                                    <!-- State Change -->
                                                    <StackPanel Grid.Column="1" 
                                                                Orientation="Horizontal"
                                                                VerticalAlignment="Center"
                                                                Margin="8,0,0,0">
                                                        <TextBlock Text="{Binding BeforeState}"
                                                                   Foreground="{DynamicResource Plc.Text.Label}"
                                                                   FontSize="12"
                                                                   VerticalAlignment="Center"/>
                                                        <TextBlock Text=" > "
                                                                   Foreground="{DynamicResource Cyber.Text.Main}"
                                                                   FontSize="12"
                                                                   Margin="4,0"
                                                                   VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding AfterState}"
                                                                   Foreground="{DynamicResource Sensor.Text.Title}"
                                                                   FontSize="12"
                                                                   FontWeight="Medium"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <!-- Message -->
                                                    <TextBlock Grid.Column="2" 
                                                               Text="{Binding Message}"
                                                               Foreground="{DynamicResource Plc.Text.Label}"
                                                               FontSize="12"
                                                               TextWrapping="Wrap"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0"/>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Event Log Cards - Two Row Layout with UserId -->
                            <ItemsControl ItemsSource="{Binding CurrentEventLogs}"
                                          Visibility="{Binding IsEventMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource LogCardStyle}" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Row 0: Timestamp + UserId (no label) -->
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Timestamp -->
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                                               Foreground="{DynamicResource Cyber.NeonBlue}"
                                                               FontSize="14"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"
                                                               VerticalAlignment="Center"/>

                                                    <!-- User ID (direct display, no label) -->
                                                    <TextBlock Grid.Column="1" 
                                                               Text="{Binding UserId}"
                                                               Foreground="{DynamicResource Sensor.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"
                                                               VerticalAlignment="Center"/>
                                                </Grid>

                                                <!-- Row 1: Event Type + Severity + Description + Message -->
                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="80"/>
                                                        <ColumnDefinition Width="250"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Event Type -->
                                                    <Border Grid.Column="0" 
                                                            CornerRadius="4"
                                                            Padding="8,4"
                                                            Margin="0,0,8,0"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Left">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#3B82F6"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding EventType}" Value="Alarm">
                                                                        <Setter Property="Background" Value="#EF4444"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EventType}" Value="Warning">
                                                                        <Setter Property="Background" Value="#F59E0B"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EventType}" Value="System Event">
                                                                        <Setter Property="Background" Value="#3B82F6"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EventType}" Value="Safety Event">
                                                                        <Setter Property="Background" Value="#8B5CF6"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding EventType}"
                                                                   Foreground="White"
                                                                   FontSize="11"
                                                                   FontWeight="Bold"
                                                                   FontFamily="Consolas"
                                                                   HorizontalAlignment="Center"/>
                                                    </Border>

                                                    <!-- Severity -->
                                                    <Border Grid.Column="1" 
                                                            CornerRadius="4"
                                                            Padding="8,4"
                                                            Margin="0,0,8,0"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Left">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#6B7280"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                                                        <Setter Property="Background" Value="#DC2626"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="Major">
                                                                        <Setter Property="Background" Value="#EA580C"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="Minor">
                                                                        <Setter Property="Background" Value="#CA8A04"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="Info">
                                                                        <Setter Property="Background" Value="#2563EB"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding Severity}"
                                                                   Foreground="White"
                                                                   FontSize="11"
                                                                   FontWeight="Bold"
                                                                   FontFamily="Consolas"
                                                                   HorizontalAlignment="Center"/>
                                                    </Border>

                                                    <!-- Event Description -->
                                                    <TextBlock Grid.Column="2" 
                                                               Text="{Binding EventDescription}"
                                                               Foreground="{DynamicResource Plc.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               VerticalAlignment="Center"
                                                               TextTrimming="CharacterEllipsis"/>

                                                    <!-- Message -->
                                                    <TextBlock Grid.Column="3" 
                                                               Text="{Binding Message}"
                                                               Foreground="{DynamicResource Plc.Text.Label}"
                                                               FontSize="12"
                                                               TextWrapping="Wrap"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0"/>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Periodic Data Log Cards -->
                            <ItemsControl ItemsSource="{Binding CurrentPeriodicDataLogs}"
                                          Visibility="{Binding IsPeriodicDataMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource LogCardStyle}" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="160"/>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="160"/>
                                                    <ColumnDefinition Width="130"/>
                                                    <ColumnDefinition Width="130"/>
                                                    <ColumnDefinition Width="130"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Timestamp (wider) -->
                                                <TextBlock Grid.Column="0" 
                                                           Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                                           Foreground="{DynamicResource Plc.Text.Value}"
                                                           FontSize="13"
                                                           FontFamily="Consolas"
                                                           VerticalAlignment="Center"/>

                                                <!-- User ID (NEW - FDA Compliance, wider) -->
                                                <TextBlock Grid.Column="1" 
                                                           Text="{Binding UserId}"
                                                           Foreground="{DynamicResource Cyber.Text.Main}"
                                                           FontSize="13"
                                                           FontWeight="Medium"
                                                           FontFamily="Consolas"
                                                           VerticalAlignment="Center"
                                                           Margin="8,0,0,0"/>

                                                <!-- Batch ID -->
                                                <TextBlock Grid.Column="2" 
                                                           Text="{Binding BatchId}"
                                                           Foreground="{DynamicResource Cyber.Text.Main}"
                                                           FontSize="13"
                                                           FontWeight="Medium"
                                                           FontFamily="Consolas"
                                                           VerticalAlignment="Center"
                                                           Margin="8,0,0,0"/>

                                                <!-- PreDry Temp -->
                                                <StackPanel Grid.Column="3" 
                                                            Orientation="Horizontal"
                                                            VerticalAlignment="Center"
                                                            Margin="8,0,0,0">
                                                    <TextBlock Text="PreDry: "
                                                               Foreground="{DynamicResource Plc.Text.Label}"
                                                               FontSize="12"/>
                                                    <TextBlock Text="{Binding PredryTemp, StringFormat='{}{0:F1} C'}"
                                                               Foreground="{DynamicResource Sensor.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"/>
                                                </StackPanel>

                                                <!-- Dry Temp -->
                                                <StackPanel Grid.Column="4" 
                                                            Orientation="Horizontal"
                                                            VerticalAlignment="Center"
                                                            Margin="8,0,0,0">
                                                    <TextBlock Text="Dry: "
                                                               Foreground="{DynamicResource Plc.Text.Label}"
                                                               FontSize="12"/>
                                                    <TextBlock Text="{Binding DryTemp, StringFormat='{}{0:F1} C'}"
                                                               Foreground="{DynamicResource Sensor.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"/>
                                                </StackPanel>

                                                <!-- CDA Pressure -->
                                                <StackPanel Grid.Column="5" 
                                                            Orientation="Horizontal"
                                                            VerticalAlignment="Center"
                                                            Margin="8,0,0,0">
                                                    <TextBlock Text="CDA: "
                                                               Foreground="{DynamicResource Plc.Text.Label}"
                                                               FontSize="12"/>
                                                    <TextBlock Text="{Binding CdaInletPressure, StringFormat='{}{0:F1} bar'}"
                                                               Foreground="{DynamicResource Sensor.Text.Title}"
                                                               FontSize="13"
                                                               FontWeight="Bold"
                                                               FontFamily="Consolas"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
