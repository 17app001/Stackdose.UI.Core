<UserControl x:Class="Stackdose.App.DeviceFramework.Controls.PlcDataGridPanel"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:Stackdose.UI.Core.Controls;assembly=Stackdose.UI.Core"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <Border Background="{StaticResource Surface.Bg.Card}"
            CornerRadius="8"
            BorderBrush="{StaticResource Surface.Border.Default}"
            BorderThickness="1"
            Padding="14,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 面板標題 -->
            <TextBlock Grid.Row="0"
                       Text="{Binding Title, ElementName=Root}"
                       Foreground="{StaticResource Text.Secondary}"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <!-- 資料格狀列表 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=Root}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource Surface.Border.Default}"
                                    BorderThickness="0,0,0,1"
                                    Padding="10,9">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- 標籤名稱（灰色小字） -->
                                    <TextBlock Grid.Row="0"
                                               Text="{Binding Label}"
                                               Foreground="{StaticResource Text.Tertiary}"
                                               FontSize="{Binding LabelFontSize, ElementName=Root}"/>

                                    <!-- PLC 數值 -->
                                    <core:PlcLabel Grid.Row="1"
                                                   Address="{Binding Address}"
                                                   DefaultValue="{Binding DefaultValue}"
                                                   FrameShape="{Binding FrameShape}"
                                                   ValueForeground="{Binding ValueColorTheme}"
                                                   ShowAddress="False"
                                                   ValueFontSize="{Binding ValueFontSize, ElementName=Root}"
                                                   Margin="0,4,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
