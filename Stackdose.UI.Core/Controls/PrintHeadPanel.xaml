<UserControl x:Class="Stackdose.UI.Core.Controls.PrintHeadPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stackdose.UI.Core.Controls"
             mc:Ignorable="d" 
              d:DesignHeight="400" d:DesignWidth="300"
              x:Name="root">

    <UserControl.Resources>
        <Style x:Key="PanelInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Plc.Bg.Dark}"/>
            <Setter Property="Foreground" Value="{DynamicResource Cyber.Text.Main}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Plc.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PrimaryActionStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Status.Warning}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Opacity="0.95">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- PrintHead Status List -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="10" Background="{DynamicResource Plc.Bg.Dark}">
            <StackPanel x:Name="PrintHeadContainer">
                <!-- PrintHead controls will be added dynamically in code-behind -->
            </StackPanel>
        </ScrollViewer>

        <!-- Flash Control Area -->
        <Border Grid.Row="1" 
                BorderBrush="#FFA726" 
                BorderThickness="2" 
                CornerRadius="6" 
                Padding="10" 
                Margin="10"
                Background="{DynamicResource Plc.Bg.Main}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Input Box -->
                <Grid Grid.Row="0" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="KHz:" 
                               FontSize="11" 
                               Foreground="{DynamicResource Cyber.Text.Muted}" 
                               VerticalAlignment="Center" 
                               Margin="0,0,8,0"/>

                    <TextBox Grid.Column="1" 
                             x:Name="FlashTimesTextBox"
                             Style="{StaticResource PanelInputStyle}"
                             Height="28" 
                             Text="0.1,1,1,1"
                             VerticalContentAlignment="Center"/>
                </Grid>

                <!-- Flash Button -->
                <Button Grid.Row="1" 
                        x:Name="FlashButton"
                        Content="Flash PrintHead" 
                        Style="{StaticResource PrimaryActionStyle}"
                        Height="32"
                        Background="{DynamicResource Button.Bg.Warning}" 
                        HorizontalAlignment="Stretch"
                        Click="FlashButton_Click"
                        Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
