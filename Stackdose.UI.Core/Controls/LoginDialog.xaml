<Window x:Class="Stackdose.UI.Core.Controls.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="User Login"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Window.Resources>
        <Style x:Key="FieldTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Cyber.Text.Muted}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource Cyber.Bg.Card}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Plc.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource Button.Bg.Info}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource Cyber.Bg.Card}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Plc.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource Button.Bg.Info}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Rectangle Fill="{DynamicResource Overlay.Bg.Scrim}" MouseDown="Overlay_MouseDown"/>

        <Border Background="{DynamicResource Plc.Bg.Main}"
                BorderBrush="{DynamicResource Button.Bg.Info}"
                BorderThickness="2"
                CornerRadius="12"
                Padding="30"
                Width="460"
                MinHeight="460"
                MaxHeight="560"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="{Binding BorderBrush.(SolidColorBrush.Color), RelativeSource={RelativeSource AncestorType=Border}}"
                                  BlurRadius="24"
                                  ShadowDepth="0"
                                  Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,24">
                    <TextBlock Text="User Login"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{DynamicResource Button.Bg.Info}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="SQLite User Authentication"
                               FontSize="12"
                               Foreground="{DynamicResource Plc.Text.Default}"
                               HorizontalAlignment="Center"
                               Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="User ID" Style="{StaticResource FieldTextStyle}"/>
                    <TextBox x:Name="UserIdTextBox"
                             Style="{StaticResource InputBoxStyle}"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="Password" Style="{StaticResource FieldTextStyle}"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource PasswordBoxStyle}"/>
                </StackPanel>

                <Border Grid.Row="2"
                        x:Name="ErrorPanel"
                        MinHeight="56"
                        Margin="0,20,0,8"
                        Background="{DynamicResource Cyber.Bg.Card}"
                        BorderBrush="{DynamicResource Status.Error}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="10"
                        Visibility="Collapsed">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ErrorText"
                                   Foreground="{DynamicResource Status.Error}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Text="Error message placeholder"/>
                    </ScrollViewer>
                </Border>

                <Border Grid.Row="2"
                        x:Name="LoadingPanel"
                        MinHeight="56"
                        Margin="0,20,0,8"
                        Background="{DynamicResource Cyber.Bg.Card}"
                        BorderBrush="{DynamicResource Button.Bg.Info}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="10"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="24" Height="24" Margin="0,0,12,0">
                            <Grid>
                                <Ellipse Width="24" Height="24"
                                         Stroke="{DynamicResource Button.Bg.Info}"
                                         StrokeThickness="3"
                                         StrokeDashArray="4,2">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="LoadingRotation" CenterX="12" CenterY="12"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                                     Storyboard.TargetProperty="Angle"
                                                                     From="0"
                                                                     To="360"
                                                                     Duration="0:0:1.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="LoadingText"
                                   Text="Logging in..."
                                   Foreground="{DynamicResource Button.Bg.Info}"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="3" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="Login"
                            IsDefault="True"
                            Style="{StaticResource DialogButtonStyle}"
                            Background="{DynamicResource Button.Bg.Info}"
                            Click="LoginButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Cancel"
                            IsCancel="True"
                            Style="{StaticResource DialogButtonStyle}"
                            Background="{DynamicResource Plc.Border}"
                            Click="CancelButton_Click"/>
                </Grid>

                <TextBlock Grid.Row="4"
                           Margin="0,12,0,0"
                           Text="Press Enter to login, Esc to cancel"
                           Foreground="{DynamicResource Plc.Text.Gray}"
                           FontSize="11"
                           HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
