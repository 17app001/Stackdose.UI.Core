<UserControl x:Class="Stackdose.UI.Core.Controls.UserManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stackdose.UI.Core.Controls"
             xmlns:converters="clr-namespace:Stackdose.UI.Core.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <converters:BoolToActiveStatusConverter x:Key="BoolToActiveStatusConverter"/>
        
        <Style x:Key="CyberDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource Cyber.Bg.Panel}"/>
            <Setter Property="Foreground" Value="{DynamicResource Cyber.Text.Bright}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Cyber.Border.Strong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#0A1A2A"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Cyber.Border}"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>

        <Style x:Key="CyberButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Cyber.NeonBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Cyber.Border.Strong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                            <Border.Effect>
                                <DropShadowEffect x:Name="Glow"
                                                Color="#00E5FF" 
                                                BlurRadius="0" 
                                                ShadowDepth="0" 
                                                Opacity="0"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Glow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="15" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="Glow"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0.6" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Glow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="Glow"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource Cyber.Bg.Dark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource Cyber.Bg.Panel}"
                BorderBrush="{DynamicResource Cyber.Border.Strong}"
                BorderThickness="0,0,0,2"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="User Management"
                             Foreground="{DynamicResource Cyber.NeonBlue}"
                             FontSize="20"
                             FontWeight="Bold"/>
                    <TextBlock x:Name="SubtitleText"
                             Text="Current: Admin"
                             Foreground="{DynamicResource Cyber.Text.Dim}"
                             FontSize="12"
                             Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" 
                          Orientation="Horizontal"
                          VerticalAlignment="Center">
                    <Button x:Name="AddUserButton"
                          Content="Add User"
                          Style="{StaticResource CyberButtonStyle}"
                          Click="AddUserButton_Click"
                          Margin="0,0,10,0"/>
                    <Button x:Name="RefreshButton"
                          Content="Refresh"
                          Style="{StaticResource CyberButtonStyle}"
                          Background="#607D8B"
                          Click="RefreshButton_Click"
                          Margin="0,0,10,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" 
                Background="{DynamicResource Cyber.Bg.Panel}"
                BorderBrush="{DynamicResource Cyber.Border}"
                BorderThickness="1"
                Margin="10">
            <DataGrid x:Name="UserDataGrid"
                    Style="{StaticResource CyberDataGridStyle}"
                    ItemsSource="{Binding Users}"
                    SelectedItem="{Binding SelectedUser}"
                    SelectionChanged="UserDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" 
                                      Binding="{Binding Id}" 
                                      Width="60"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="User ID" 
                                      Binding="{Binding UserId}" 
                                      Width="150"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Display Name" 
                                      Binding="{Binding DisplayName}" 
                                      Width="180"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Access Level" 
                                      Binding="{Binding AccessLevel}" 
                                      Width="120"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Status" 
                                      Binding="{Binding IsActive, Converter={StaticResource BoolToActiveStatusConverter}}" 
                                      Width="80"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Department" 
                                      Binding="{Binding Department}" 
                                      Width="120"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Email" 
                                      Binding="{Binding Email}" 
                                      Width="*"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Created By" 
                                      Binding="{Binding CreatedBy}" 
                                      Width="120"
                                      IsReadOnly="True"/>
                    <DataGridTextColumn Header="Created At" 
                                      Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" 
                                      Width="140"
                                      IsReadOnly="True"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource Cyber.Text.Bright}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Cyber.NeonBlue}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A4A5F7F"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource Cyber.Bg.Dark}"/>
                        <Setter Property="Foreground" Value="{DynamicResource Cyber.NeonBlue}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource Cyber.Border.Strong}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,2"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource Cyber.Bg.Panel}"
                BorderBrush="{DynamicResource Cyber.Border.Strong}"
                BorderThickness="0,2,0,0"
                Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="EditUserButton"
                      Content="Edit"
                      Style="{StaticResource CyberButtonStyle}"
                      Background="#4CAF50"
                      Click="EditUserButton_Click"
                      IsEnabled="False"
                      Margin="0,0,10,0"/>
                <Button x:Name="ResetPasswordButton"
                      Content="Reset Password"
                      Style="{StaticResource CyberButtonStyle}"
                      Background="#FF9800"
                      Click="ResetPasswordButton_Click"
                      IsEnabled="False"
                      Margin="0,0,10,0"/>
                <Button x:Name="ToggleActiveButton"
                      Content="Disable"
                      Style="{StaticResource CyberButtonStyle}"
                      Background="#F44336"
                      Click="ToggleActiveButton_Click"
                      IsEnabled="False"
                      Margin="0,0,10,0"/>
                <Button x:Name="ViewAuditButton"
                      Content="View Audit"
                      Style="{StaticResource CyberButtonStyle}"
                      Background="#9C27B0"
                      Click="ViewAuditButton_Click"
                      IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
