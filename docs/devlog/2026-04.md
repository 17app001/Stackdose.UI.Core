# 開發日誌 — 2026 年 4 月

> 格式說明：`feat` 新功能 / `fix` 修正 / `refactor` 重構 / `chore` 維護
> 跨 Repo 異動標記：`[Platform ↔ UI.Core]`

---

## 2026-04-17

**feat** SecuredButton — writeValue / pulse / toggle 擴充
- 新增 `writeValue`（自訂寫入值）、`commandType`（write / pulse / toggle）、`pulseMs`（pulse 延遲）
- ProcessCommandService 新增 5 參數 writeValue overload
- DesignerItemViewModel 新增 WriteValue / CommandType / PulseMs 屬性
- PropertyPanel SecuredButtonTemplate 完整 UI（writeValue 輸入、commandType 下拉、pulseMs 滑桿）
- ToolboxItemDescriptor SecuredButton 預設值更新

**fix** RuntimeControlFactory — commandAddress 讀取 bug 修復
- DesignPlayer + DesignRuntime 兩處 RuntimeControlFactory 修正
- 原先讀 `"address"` 但設計器存 `"commandAddress"`，導致 Runtime 按鈕無 PLC 位址
- 改為優先讀 `commandAddress`，fallback 到 `address`（向後相容）

**feat** AlarmViewer — MachinePageDesigner 行內編輯器
- 新增 AlarmEditItem 編輯模型（group / device / bit / description）
- DesignerItemViewModel 新增 AlarmItems ObservableCollection，自動同步至 Props dict
- PropertyPanel AlarmViewerTemplate 完整行內 UI（新增/刪除/修改）
- DesignFileService.Save 存檔時自動輸出 alarms.json（{"alarms":[...]} 格式）
- DesignTimeControlFactory 預覽顯示嵌入項目數量
- PropsExtensions 新增 GetObjectList 輔助方法（支援 JsonElement + 原生 List）

**feat** SensorViewer — MachinePageDesigner 行內編輯器
- 新增 SensorEditItem 編輯模型（group / device / bit / value / mode / description）
- 同 AlarmViewer 架構，PropertyPanel 完整行內 UI
- 支援 AND / OR / COMPARE 三種模式下拉選擇
- DesignFileService.Save 存檔時自動輸出 sensors.json（扁平陣列格式）
- SensorContext 實作驗證完畢（LoadFromJson + GenerateMonitorAddresses + 狀態事件）
- sensors.json 格式已確認並文件化

**chore** 文件更新
- CLAUDE.md 已知缺口表更新（AlarmViewer/SensorViewer/SecuredButton → ✅ 已補強）
- index.html 缺口區段改為完成紀錄，路線圖標記已完成項，設計師能力清單更新
- devlog 新增本日記錄

---

## 2026-04-16（續三）

**feat** DesignViewer — 多頁面切換
- 同步 DesignRuntime 架構：`LoadFile` → `BuildPageTabs` + `SwitchPage` → `RenderPage(DesignPage)`
- `MainWindow.xaml` 新增 Row 1（`pageTabsBar`）：單頁 Collapsed，多頁 Visible
- 使用 `DesignTimeControlFactory`（設計時靜態控制項，無 PLC 連線）

---

## 2026-04-16（續二）

**feat** DesignPlayer — 多頁面切換（MonitorPage）
- 同步 DesignRuntime 架構：`RenderDocument` → `BuildPageTabs` + `SwitchPage` → `RenderPage(DesignPage)`
- `MonitorPage.xaml` 新增 Row 1（`PageTabsBar`）：單頁 Collapsed，多頁 Visible
- 頁籤樣式使用設計系統 Token（`Surface.*` / `Action.*` Resource），Fallback 至深色預設值
- Hot-reload 保留當前頁索引

---

## 2026-04-16（續）

**feat** DesignRuntime — 多頁面切換
- 讀取 `.machinedesign.json` 的 `pages` 陣列（v2.0 格式），不再只讀頂層 legacy 欄位
- 新增頂部頁籤列（`pageTabsBar`）：單頁文件自動隱藏，多頁文件顯示頁名按鈕
- 點擊頁籤切換畫布內容（`SwitchPage(index)`），當前頁高亮藍色
- Hot-reload（JSON 儲存觸發）保留當前頁索引，頁數縮減時退回最後一頁
- `RenderDocument` 拆為：建頁籤 → `SwitchPage` → `RenderPage(DesignPage)`

---

## 2026-04-16

**feat** DesignRuntime — PLC 斷線重連穩定性
- `PlcStatus`（UI.Core）新增 `ConnectionLost` 事件，看門狗偵測到斷線時觸發
- 斷線瞬間 StatusText 立即改為 `RECONNECTING...`，UpdateUiState 設為 Failed
- DesignRuntime 訂閱 `ConnectionLost`：顯示 `⚠ PLC 斷線，正在重新連線…`，暫時禁用 Disconnect 按鈕（避免重連途中的操作衝突）
- DesignRuntime `ConnectionEstablished` handler 新增 `RefreshMonitors()` 呼叫，確保重連後畫布上的 PlcLabel 地址重新納入掃描清單

**feat** MachinePageDesigner — 多頁面支援（JSON v2.0）
- 新增 `Models/DesignPage.cs`：`pageId / name / canvasWidth / canvasHeight / canvasItems`
- `DesignDocument` 升級 v2.0：新增 `pages` 陣列，保留 `canvasItems / canvasWidth / canvasHeight` 做 Runtime 向後相容
- `DesignFileService.Load`：自動正規化（v1.0 舊檔 / 空文件 → 轉為單頁 "Main"，無須手動遷移）
- `DesignFileService.Save`：儲存前同步 Legacy 欄位至 `pages[0]`（DesignRuntime / DesignPlayer 讀第一頁，不需改動）
- `DesignCanvasViewModel` 新增 `LoadFromPage()` / `ExportToPage()` 方法
- 新增 `ViewModels/PageTabViewModel`：每頁有獨立 Canvas + UndoRedo（Ctrl+Z 不跨頁面）
  - `IsActive`（目前選中頁）、`IsEditing`（內嵌重命名）
- `MainViewModel` 重構：`Canvas` / `UndoRedo` 改為代理當前頁面的屬性
  - `SetCurrentPage()`：切換頁面時自動解除舊訂閱、重新訂閱 Canvas.PropertyChanged + UndoRedo.StateChanged
  - 新增 `AddPageCmd / DeletePageCmd / SelectPageCmd / RenamePageCmd` 四個指令
- `MainWindow.xaml` 新增頁籤列（Toolbar 與畫布之間）：
  - 點擊切換頁面（IsActive 觸發高亮樣式）
  - 雙擊進入內嵌重命名（TextBox + FocusHelper 自動取得焦點）
  - `×` 按鈕刪除頁面（保護：至少留一頁）
  - `+` 按鈕新增頁面
- 新增 `Views/Converters.cs`：`InverseBoolToVisibilityConverter` + `FocusHelper` attached property

**fix** 設計器細節（補上一個 session 遺漏的 commit）
- `DesignTimeControlFactory`：`CreatePlcStatusIndicator` 讀取 `label` prop
- `DesignTimeControlFactory`：`CreateViewerPlaceholder` 顯示 `configFile` 檔名
- `ToolboxItemDescriptor`：`PlcStatusIndicator` 補 label 預設值、`AlarmViewer / SensorViewer` 補 configFile 欄位、Spacer DisplayName 改 GroupBox

---

## 2026-04-15（續）

**feat** MachinePageDesigner 控制項補全 + 設計體驗改善

- 新增 `StaticLabel` 控制項（靜態文字，適合標題與說明）
  - ToolboxItemDescriptor 加入（Category: Layout，Icon: T）
  - DesignerItemViewModel 加入 6 個新屬性：StaticText / StaticFontSize / StaticFontWeight / StaticTextAlign / StaticForeground
  - DesignTimeControlFactory + 兩個 RuntimeControlFactory 均支援
  - PropertyPanel 新增 StaticLabelTemplate（文字 + 大小滑桿 + 粗細 + 對齊 + 顏色）
- 補全 LiveLog / AlarmViewer / SensorViewer 屬性面板 DataTemplate
  - LiveLog：說明性文字（無可設定屬性）
  - AlarmViewer / SensorViewer：configFile 路徑輸入
- 新增 FreeCanvasItem 右鍵 ContextMenu
  - 複製、貼上、刪除、鎖定/解鎖、移到最前/後、上/下移一層
  - `MouseRightButtonDown` 處理右鍵自動選取
- PropertyPanelTemplateSelector 完整支援全部 9 種控制項類型

**feat** DesignPlayer 設定頁面（Settings Page）
- 新增 `Pages/SettingsPage.xaml` + `.cs`：PLC IP / Port / 輪詢間隔 / 自動連線 / 設計稿路徑 / App 標題 / 設備標籤
- `PlayerConfigLoader.Save()` — 將 PlayerAppConfig 序列化並寫回 app-config.json
- MainWindow 加入 Settings 導航項目（Operator 以上）
- SettingsPage.ApplyRequested 事件 → MainWindow 即時更新 Shell 標題

**docs** index.html 補全
- 統計數字修正：設計器工具 3 → 4
- DesignRuntime 進度面板：JSON Hot-Reload 標記為完成（7/8）
- 時間軸：4月15日條目更新，近期條目改為剩餘工作
- 新增「快速上手」4步驟工作流程 + Toolbox 控制項速查
- 新增「開發藍圖」三欄（已完成 / 進行中 / 計畫中）

---

## 2026-04-15

**feat** JSON Hot-Reload（DesignRuntime + DesignPlayer）
- 兩個執行專案均加入 `FileSystemWatcher`，偵測 .machinedesign.json 變更自動重載畫布
- 防抖機制：800ms 內同路徑只觸發一次，延遲 400ms 讀取等待寫入完成
- DesignRuntime 新增 `OnWindowClosing` handler，統一清理 watcher / PLC / ComplianceContext
- DesignPlayer 熱更新後顯示 `↺ 檔名` 提示
- 修正 `designer-system.md` 的錯誤 JSON schema（`canvasItems` 非 `items`，尺寸在頂層非 `meta` 下）
- 新增 DesignPlayer 完整章節至 `designer-system.md`（含與 DesignRuntime 差異表）

**feat** 新增 `Stackdose.App.DesignPlayer` — 量產交付 Shell App
- 定位：MachinePageDesigner → .machinedesign.json → **DesignPlayer**（現場部署用）
- 完整 Shell UI：左側導航、頁首設備名稱、頁尾狀態列（MainContainer）
- 外部 JSON 設定（`Config/app-config.json`）：PLC IP、輪詢間隔、登入開關、畫布檔路徑
- 可選登入對話框（`loginRequired: true`）+ 使用者管理頁面（Operator 以上）
- `RuntimeControlFactory` 依 type 建立 live PLC 控制項（8 種類型）
- 加入 `Stackdose.UI.Core.sln`（🚀 開發中）與 `Stackdose.Designer.sln`
- 補充：`README.md`、`CLAUDE.md` 專案表、`index.html` 專案卡片

**docs** 建立專案總覽 Web 頁面（index.html）
- 新增根目錄 `index.html`，單一靜態頁面，無外部依賴
- 涵蓋：架構圖（四層）、11 個專案卡片（含狀態燈）、12 篇文件連結、設計器工作流程圖
- 新增開發進度區塊：4 個功能完成度面板（進度條）+ 時間軸
- 專案卡片點擊彈出 Modal，顯示詳細功能清單與文件連結
- 主題改為 Light，適合主管簡報場景

**docs** 補齊文件缺口（主管評審 88 分後改善）
- 新增 `docs/kb/deviceframework-guide.md`（DeviceFramework 新人完整指南，取代亂碼舊文件）
  - 涵蓋：設定格式說明、最小必要欄位、啟動程式碼、擴充決策表、常見問題
- 翻譯 `docs/kb/design-standard.md`（英文 → 中文）
- 翻譯 `docs/kb/theme-token-standard.md`（英文 → 中文）
- 更新 `architecture.md` 技術債條目，移除「以程式碼為準」，改指向新指南

**docs** 補全所有專案 README（8 個）
- `Stackdose.App.ShellShared/README.md`
- `Stackdose.App.UbiDemo/README.md`
- `Stackdose.App.DesignRuntime/README.md`
- `Stackdose.Tools.MachinePageDesigner/README.md`
- `Stackdose.Tools.DesignViewer/README.md`
- `Stackdose.Tools.ProjectGenerator/README.md`
- `Stackdose.Tools.ProjectGeneratorUI/README.md`
- `Stackdose.App.DeviceFramework/README.md`

---

## 2026-04-14

**feat** DesignRuntime 新增模擬器模式與亂數 D100~D102 測試注入
- 新增「模擬器模式」CheckBox（啟用後不需真實 PLC 即可連線）
- 新增「亂數 D100~D102」按鈕（每 100~300ms 隨機寫入測試值，Toggle 開關）
- 修正斷線時未清除 `PlcContext.GlobalStatus` 問題
- 載入 JSON 後呼叫 `RefreshMonitors()` 確保新地址加入掃描清單

**chore** 方案分組整理 + 聚焦方案 + 目前焦點文件
- `Stackdose.UI.Core.sln` 加入 Solution Folder 分組（5個群組）
- 新增 `Stackdose.Designer.sln`（設計器聚焦方案，14個專案，每日開發用）
- 新增 `CURRENT_FOCUS.md`（當下焦點 + 各專案一句話說明）

**docs** 建立知識庫系統
- 新增 `CLAUDE.md`（根目錄快速上下文）
- 新增 `docs/PROJECT_MAP.md`（完整依賴圖）
- 新增 `docs/kb/`（架構、設計器、Platform契約、控制項參考）
- 新增 `docs/devlog/`（開發日誌月份檔案）
- 新增 `docs/MANAGER_BRIEF.md`（技術主管進度簡報）
- 清除 7 個散落冗餘的 .md 檔案

---

## 2026-04-13

**feat** 新增 DesignRuntime 真實執行專案（PLC 連線 + JSON 載入）
- 新增 `Stackdose.App.DesignRuntime` 專案至方案
- 支援載入 `.machinedesign.json` 並連線真實 PLC 顯示即時數值

**feat** 新增 DesignViewer 測試專案（拖入 JSON 即時預覽畫布）
- 新增 `Stackdose.Tools.DesignViewer` 專案至方案
- 拖入 `.machinedesign.json` 即時渲染，不需連 PLC

**feat** 設計器全功能整合（複製貼上 / 多選同步 / GroupBox / 對齊分配）
- MachinePageDesigner 新增 Ctrl+C / Ctrl+V 複製貼上
- 多選後同步移動
- 支援 GroupBox 群組化
- 新增對齊與均等分配功能

**feat** MachinePageDesigner 全面改為自由畫布拖曳設計器
- 正式棄用 Zone 制模式，統一改為 FreeCanvas 模式
- 控制項可拖曳至畫布任意位置

**feat** 畫布尺寸設定 + 修正儲存讀取與 Undo 正確性
- 新增畫布寬高設定功能
- 修正多個儲存/載入後狀態不一致問題
- Undo/Redo 正確性改善

**fix** FreeCanvasItem 無法移動且卡死問題
- 修正拖曳事件捕獲導致的卡死 bug

**feat** 自由畫布新增 Snap / Z-Order / 框選 / 鎖定四大功能
- Snap：拖曳時自動吸附格線
- Z-Order：調整控制項層級（前置/後置）
- 框選：拖曳框選多個控制項
- 鎖定：防止已完成控制項被誤移

**refactor** MachinePageDesigner 統一改為自由畫布（FreeCanvas）模式
- 移除 Zone 制 Grid 排列邏輯
- 以 `FreeCanvasItem` 包裝所有控制項

---

## 2026-04-08

**feat** MachinePageDesigner 完成拖曳體驗與 DeviceFramework 整合
- 拖曳操作體驗優化（MouseCapture、Cursor 反饋）
- 與 DeviceFramework 的 `DeviceLabelViewModel` 整合

**feat** 實作自由畫布設計器（FreeCanvas）完整功能
- FreeCanvas 首版完整功能實作
- 包含：放置、移動、刪除、屬性面板編輯

---

## 2026-04-07

**fix** 完成 bug 修正
- 修正數個設計器操作相關問題（詳情見 commit 89fc943）

---

## 2026-04-01

**feat** 抽出 PlcDataGridPanel 通用元件，取代 DynamicDevicePage 內嵌 panel
- 將原本嵌在 `DynamicDevicePage` 的 Labels 面板抽成獨立 `PlcDataGridPanel` 控制項
- 可在其他頁面重用

**feat** ProjectGeneratorUI 預覽改用真實 PlcDataGridPanel 顯示 Labels 資料
- 產生器預覽頁面改用真實控制項渲染，更貼近實際效果

**fix** ProjectGeneratorUI App.xaml 補上 UI.Core Theme.xaml 資源合併
- 修正 ProjectGeneratorUI 缺少主題資源導致控制項樣式異常

**docs** 新增 MachinePageDesigner 架構書（初版規劃文件）

**chore** 新增 `Stackdose.Tools.MachinePageDesigner` 專案（初始化，Zone制模式，開發中）

**chore** 清理冗餘說明檔與備份腳本，新增方案說明文件
