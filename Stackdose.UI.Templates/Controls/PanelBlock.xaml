<UserControl x:Class="Stackdose.UI.Templates.Controls.PanelBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignWidth="320"
             d:DesignHeight="200">

    <Border Style="{StaticResource Template.PanelCard}"
            Padding="{Binding BlockPadding, ElementName=Root}">
        <StackPanel>
            <TextBlock Text="{Binding Title, ElementName=Root}"
                       Margin="0,0,0,10">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource Template.SectionTitleText}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Title, ElementName=Root}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ContentPresenter Content="{Binding BlockContent, ElementName=Root}"/>
        </StackPanel>
    </Border>
</UserControl>
