<Window x:Class="ModelB.Demo.FdaTestDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FDA Test Data Generator" 
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1177BB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#888"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="FDA 21 CFR Part 11 Test Data Generator" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Parameter Settings -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Generation Parameters" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>

            <!-- Event Log Count -->
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Event Log Count:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="EventLogCountTextBox" Text="100"/>
            </Grid>

            <!-- Periodic Data Log Count -->
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Periodic Data Log Count:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="PeriodicDataCountTextBox" Text="500"/>
            </Grid>

            <!-- Days Back -->
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Days Back:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="DaysBackTextBox" Text="7"/>
            </Grid>

            <!-- Batch ID (Optional) -->
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Batch ID (Optional):" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="BatchIdTextBox" Text=""/>
            </Grid>
        </StackPanel>

        <!-- Output Log -->
        <Border Grid.Row="2" 
                BorderBrush="#3F3F46" 
                BorderThickness="1" 
                Background="#252526"
                Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock" 
                           Foreground="#CCCCCC"
                           FontFamily="Consolas"
                           FontSize="12"
                           Padding="10"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Button Area -->
        <UniformGrid Grid.Row="3" Columns="3" HorizontalAlignment="Center">
            <Button Content="Generate Event Logs" 
                    Click="GenerateEventLogs_Click" 
                    Margin="5"/>
            <Button Content="Generate Periodic Data" 
                    Click="GeneratePeriodicData_Click" 
                    Margin="5"/>
            <Button Content="Generate All Test Data" 
                    Click="GenerateAll_Click" 
                    Margin="5"
                    Background="#2E7D32"/>
        </UniformGrid>
    </Grid>
</Window>
