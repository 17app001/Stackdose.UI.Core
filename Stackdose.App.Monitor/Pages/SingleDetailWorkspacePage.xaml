<UserControl x:Class="Stackdose.App.Monitor.Pages.SingleDetailWorkspacePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="http://schemas.stackdose.com/wpf"
             xmlns:templateControls="clr-namespace:Stackdose.UI.Templates.Controls;assembly=Stackdose.UI.Templates"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Stackdose.UI.Templates;component/Resources/CommonColors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource Surface.Bg.Page}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Status Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource Surface.Bg.Panel}"
                BorderBrush="{DynamicResource Surface.Border.Default}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            VerticalAlignment="Center"
                            Margin="0,0,20,0">
                    <TextBlock Text="PLC 監控"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="MachineSummaryText"
                               Margin="0,2,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"/>
                </StackPanel>

                <core:PlcStatus x:Name="TopPlcStatus"
                                Grid.Column="2"
                                Height="58"
                                IsGlobal="True"
                                ShowBorder="True"
                                AutoConnect="False"/>
            </Grid>
        </Border>

        <!-- 4 PlcLabel Cards -->
        <Border Grid.Row="1"
                Background="{DynamicResource Surface.Bg.Panel}"
                BorderBrush="{DynamicResource Surface.Border.Default}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <UniformGrid Rows="1" Columns="4">
                <core:PlcLabel Label="壓差"
                               Address="D103"
                               DataType="Word"
                               Margin="8"/>
                <core:PlcLabel Label="流量"
                               Address="D104"
                               DataType="Word"
                               Margin="8"/>
                <core:PlcLabel Label="供量壓力"
                               Address="D105"
                               DataType="Word"
                               Margin="8"/>
                <core:PlcLabel Label="回量壓力"
                               Address="D106"
                               DataType="Word"
                               Margin="8"/>
            </UniformGrid>
        </Border>
    </Grid>
</UserControl>
